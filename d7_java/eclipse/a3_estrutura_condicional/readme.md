# IF-ELSE
	
## SINTAXE SIMPLES
```
if(<condição>) {
	<comando>
}
```
		
## SINTAXE COMPOSTA
```
if(<condição>) {
	<comando>
} else {
	<comando>
}
```

# OPERADORES DE ATRIBUIÇÃO CUMULATIVA

```
a += b;
a -= b;
a *= b;
a /= b;
a %= b;
```

# INFORMAÇÕES EXTRAS

ctrl + shift + f: realiza identação automática.